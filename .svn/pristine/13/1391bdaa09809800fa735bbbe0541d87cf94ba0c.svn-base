// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.dataom.app.web;

import com.dataom.app.dao.DistributionDao;
import com.dataom.app.domain.Distribution;
import com.dataom.app.web.DistributionController;
import java.io.UnsupportedEncodingException;
import java.math.BigInteger;
import javax.servlet.http.HttpServletRequest;
import javax.validation.Valid;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.ui.Model;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;
import org.springframework.web.bind.annotation.RequestParam;
import org.springframework.web.util.UriUtils;
import org.springframework.web.util.WebUtils;

privileged aspect DistributionController_Roo_Controller {
    
    @Autowired
    DistributionDao DistributionController.distributionDao;
    
    @RequestMapping(method = RequestMethod.POST, produces = "text/html")
    public String DistributionController.create(@Valid Distribution distribution, BindingResult bindingResult, Model uiModel, HttpServletRequest httpServletRequest) {
        if (bindingResult.hasErrors()) {
            populateEditForm(uiModel, distribution);
            return "distributions/create";
        }
        uiModel.asMap().clear();
        distributionDao.save(distribution);
        return "redirect:/distributions/" + encodeUrlPathSegment(distribution.getId().toString(), httpServletRequest);
    }
    
    @RequestMapping(params = "form", produces = "text/html")
    public String DistributionController.createForm(Model uiModel) {
        populateEditForm(uiModel, new Distribution());
        return "distributions/create";
    }
    
    @RequestMapping(value = "/{id}", produces = "text/html")
    public String DistributionController.show(@PathVariable("id") BigInteger id, Model uiModel) {
        uiModel.addAttribute("distribution", distributionDao.findOne(id));
        uiModel.addAttribute("itemId", id);
        return "distributions/show";
    }
    
    @RequestMapping(produces = "text/html")
    public String DistributionController.list(@RequestParam(value = "page", required = false) Integer page, @RequestParam(value = "size", required = false) Integer size, Model uiModel) {
        if (page != null || size != null) {
            int sizeNo = size == null ? 10 : size.intValue();
            final int firstResult = page == null ? 0 : (page.intValue() - 1) * sizeNo;
            uiModel.addAttribute("distributions", distributionDao.findAll(new org.springframework.data.domain.PageRequest(firstResult / sizeNo, sizeNo)).getContent());
            float nrOfPages = (float) distributionDao.count() / sizeNo;
            uiModel.addAttribute("maxPages", (int) ((nrOfPages > (int) nrOfPages || nrOfPages == 0.0) ? nrOfPages + 1 : nrOfPages));
        } else {
            uiModel.addAttribute("distributions", distributionDao.findAll());
        }
        return "distributions/list";
    }
    
    @RequestMapping(method = RequestMethod.PUT, produces = "text/html")
    public String DistributionController.update(@Valid Distribution distribution, BindingResult bindingResult, Model uiModel, HttpServletRequest httpServletRequest) {
        if (bindingResult.hasErrors()) {
            populateEditForm(uiModel, distribution);
            return "distributions/update";
        }
        uiModel.asMap().clear();
        distributionDao.save(distribution);
        return "redirect:/distributions/" + encodeUrlPathSegment(distribution.getId().toString(), httpServletRequest);
    }
    
    @RequestMapping(value = "/{id}", params = "form", produces = "text/html")
    public String DistributionController.updateForm(@PathVariable("id") BigInteger id, Model uiModel) {
        populateEditForm(uiModel, distributionDao.findOne(id));
        return "distributions/update";
    }
    
    @RequestMapping(value = "/{id}", method = RequestMethod.DELETE, produces = "text/html")
    public String DistributionController.delete(@PathVariable("id") BigInteger id, @RequestParam(value = "page", required = false) Integer page, @RequestParam(value = "size", required = false) Integer size, Model uiModel) {
        Distribution distribution = distributionDao.findOne(id);
        distributionDao.delete(distribution);
        uiModel.asMap().clear();
        uiModel.addAttribute("page", (page == null) ? "1" : page.toString());
        uiModel.addAttribute("size", (size == null) ? "10" : size.toString());
        return "redirect:/distributions";
    }
    
    void DistributionController.populateEditForm(Model uiModel, Distribution distribution) {
        uiModel.addAttribute("distribution", distribution);
    }
    
    String DistributionController.encodeUrlPathSegment(String pathSegment, HttpServletRequest httpServletRequest) {
        String enc = httpServletRequest.getCharacterEncoding();
        if (enc == null) {
            enc = WebUtils.DEFAULT_CHARACTER_ENCODING;
        }
        try {
            pathSegment = UriUtils.encodePathSegment(pathSegment, enc);
        } catch (UnsupportedEncodingException uee) {}
        return pathSegment;
    }
    
}
